# OKOK TAXI Bill Generator — A4 Updated

This version fixes the PDF/print layout shown in the reference:
- Exact A4 portrait bill: 210mm × 297mm
- Header/logo/company title alignment improved
- Bill title centered
- Compact table spacing so the bill fits one A4 page
- PDF export configured for A4 and one-page layout
- Browser Print configured for A4 with zero margins
- PNG export retained
- Raw-data parser and manual entry retained

## GitHub Pages
Upload `index.html`, `style.css`, `script.js`, and `logo.png` to the repository root.
Then: GitHub → Settings → Pages → Deploy from a branch → `main` → `/ (root)` → Save.

Replace `logo.png` with your original high-resolution OK OK TAXI logo if available.
